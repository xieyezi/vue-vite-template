export interface AboutState {
	aboutInfo: string
}

const state: AboutState = {
	aboutInfo: 'about info'
}
const getters = {}
const mutations = {}
const actions = {}

export default {
	namespaced: true,
	state,
	getters,
	mutations,
	actions
}
