export interface AboutDetailState {
	title: string
}

const state = {
	title: 'title from about detail state model.'
}
const getters = {}
const mutations = {}
const actions = {}

export default {
	namespaced: true,
	state,
	getters,
	mutations,
	actions
}
